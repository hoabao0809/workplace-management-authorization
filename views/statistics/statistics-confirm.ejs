<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/assets/css/<%= css %>.css">
</head>

<body>
<%- include('../includes/navigation.ejs') %>

<div class="attendance-content">
    <strong><h2>Danh sách nhân viên quản lý</h2></strong>
   
    <table>
        <thead>
            <tr>
                <th>STT</th>
                <th>Họ tên</th>
                <th>Dữ liệu giờ làm</th>
            </tr>
        </thead>
    <% if (users) { %>
        <% users.map((user, index) => { %>
        <tbody>
            <tr>
                <td><%= index + 1 %></td>
                    <td><%= user.name %></td>
                    <td><a href="/admin/statistics-confirm/<%= user._id %>"><i class="fa fa-info-circle"></i></a></td>
            </tr>
        </tbody>
        <% }) %>    
    <% } %>    
    </table>
</div>        

<%- include('../includes/footer.ejs') %>